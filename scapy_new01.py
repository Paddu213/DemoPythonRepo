"""
import scapy.all as scapy
request=scapy.ARP()
"""
import os

import scapy.all as scapy
request=scapy.ARP()
print(request.summary())
#print(os.system('ipconfig'))
import scapy.all as scapy
request=scapy.ARP()
print(request.show())

